# openagents

Workspace for agent experiments, project notes, automation scripts, and prototype applications.

This repository intentionally ignores local secrets, runtime caches, build output, logs, media captures, and databases so the public repo stays clean and reproducible.
